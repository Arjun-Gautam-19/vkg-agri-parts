# macOS
.DS_Store
._*
.AppleDouble
.LSOverride

# Editors / IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# Node (in case anything is added later)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Logs
*.log

# Env / secrets — none today, but keep it safe by default
.env
.env.local
.env.*.local

# Build output (none currently, but reserved)
dist/
build/

# Local backup of the pre-edited hero video — not needed for deploy
assets/hero-scroll.original.mp4
